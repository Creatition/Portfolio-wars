{
  "name": "portfolio-wars",
  "version": "0.1.0",
  "private": true,
  "description": "Portfolio Wars — PulseChain portfolio competition platform. Monorepo.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace @pw/shared",
    "dev:web": "npm run build:shared && npm run dev --workspace @pw/web",
    "dev:api": "npm run build:shared && npm run dev --workspace @pw/api",
    "build:web": "npm run build:shared && npm run build --workspace @pw/web",
    "build:api": "npm run build:shared && npm run build --workspace @pw/api",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.17"
  }
}
